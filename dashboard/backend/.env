# The MONGO_URL constant defines the MongoDB connection URL used to connect to the database.
# It includes the username, password, hostname, and database name.
MONGO_Url = mongodb+srv://username:password@cluster0.ssw26.mongodb.net/PREDICTION_DB?retryWrites=true&w=majority

# The PORT constant defines the port number on which the server will listen for incoming requests.
PORT=5000
